# VPU top-level Makefile — delegates to exp/
.PHONY: all test clean help format format-check kernels

all test clean help format format-check kernels libengine.a go-build go-test go-all:
	$(MAKE) -C exp $@
